export type ResponseDataAuthService = {
    email: string,
    name: string,
    referer: string,
}

export type ResponseGetDataDomain = {
    token: string,
    referer: string,
}

export type ActiveToken = {
    active: boolean,
};
